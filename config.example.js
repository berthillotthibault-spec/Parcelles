/*
 * Copiez ce fichier en config.js uniquement si vous activez un service externe.
 * Ne placez JAMAIS une clé Gemini/OpenAI privée dans ce fichier public.
 */
window.PARCELLES_FIREBASE_CONFIG = null;
// Exemple de configuration Firebase publique (apiKey Firebase côté client n'est pas un secret,
// mais les règles Firestore/Auth doivent être strictes) :
// window.PARCELLES_FIREBASE_CONFIG = { apiKey:'...', authDomain:'...', projectId:'...' };
